

{% block body %}
    <h1>Edit Solde</h1>

    {{ include('solde/_form.html.twig', {'button_label': 'Update', 'attr': {'class': 'btn btn-warning d-grid w-100'}}) }}

    <a href="{{ path('app_solde_index') }}">back to list</a>

    {{ include('solde/_delete_form.html.twig') }}
{% endblock %}
