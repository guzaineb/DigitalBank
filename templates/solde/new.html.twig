{% extends 'back/index.html.twig' %}



{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">Ajouter  un nouveau Solde</h1>

    <div class="card p-4">
        <div class="card-body">
            {{ include('solde/_form.html.twig') }}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_solde_index') }}" class="btn btn-primary"><i class="bi bi-arrow-left">Retour à la liste</a>
    </div>
</div>
{% endblock %}
